name: 'Nightly Merge'

on:
  push:
    branches:
      - master

jobs:
  nightly-merge:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Nightly Merge
      uses: robotology/gh-action-nightly-merge@v1.3.3
      with:
        stable_branch: 'master'
        development_branch: 'first-commit'
        allow_ff: false
      env:
        GITHUB_TOKEN: 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